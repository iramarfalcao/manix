const generate = () => {
    console.log("Started generate ======================================================================");
    console.log("Finished generate =====================================================================");
}

export default generate;